using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SendFileManager : MonoBehaviour
{


    /*byte[] fileData = File.ReadAllBytes("<>");


       string filePath = ""; 

            if (File.Exists(filePath))
            {
                byte[] fileData = File.ReadAllBytes();

                // Mengirim file melalui Agora
                agoraEngine.SendStreamMessage(userId, );
                statusText.text = "File terkirim!";
            }
            else
            {
                Debug.LogError("File tidak ditemukan.");
                statusText.text = "File tidak ditemukan.";
            }
        }
        }*/


}
